{
	"variables": {
		"name": "Packer_build_name",
		"aws_region": "eu-west-1",
		"aws_source_ami": "ami-971238f1",
		"aws_instance_type": "t2.nano",
		"aws_volume_size": "8",
		"ssh_port": "22",
		"ssh_username": "ubuntu",
		"ssh_timeout": "120s",
		"aws_access_key": "AKIAJ7YWOHFHGW3N7ANA",
		"aws_secret_key": "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",
		"most_recent": "false"
	},
	"builders": [
		{
			"name": "amazon",
			"access_key": "{{ user `aws_access_key` }}",
			"secret_key": "{{ user `aws_secret_key` }}",
			"ami_name": "{{ user `name` }}-{{ timestamp }}",
			"type": "amazon-ebs",
			"region": "{{ user `aws_region` }}",
			"source_ami_filter": {
				"filters": {
					"name": "UbuntuServer16.04.2-amd64*"
				},
				"most_recent": "{{ user `most_resent` }}"
			},
			"instance_type": "{{ user `aws_instance_type` }}",
			"launch_block_device_mappings": [
				{
					"delete_on_termination": true,
					"volume_type": "gp2",
					"device_name": "/dev/sda1",
					"volume_size": "{{ user `aws_volume_size` }}"
				}
			],
			"ssh_port": "{{ user `ssh_port` }}",
			"ssh_username": "{{ user `ssh_username` }}",
			"ssh_timeout": "{{ user `ssh_timeout` }}"
		}
	]
}
